<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="TouchQuickAction.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TouchQuickAction"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="设置">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image Source="/Assets/StoreLogo.png" Grid.Column="1" Width="24" Height="24" VerticalAlignment="Center"/>
            <TextBlock Text="设置" Grid.Column="2" Margin="10,14"/>
            <Grid x:Name="titleBar" Grid.Column="1" Grid.ColumnSpan="3"/>
        </Grid>
        <NavigationView>
            
        </NavigationView>
    </Grid>
</Window>
