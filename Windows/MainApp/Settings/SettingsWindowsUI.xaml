<?xml version="1.0" encoding="utf-8"?>
<Grid
    x:Class="MainApp.Settings.SettingsWindowsUI"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MainApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid.KeyboardAccelerators>
        <KeyboardAccelerator Key="Left" Modifiers="Menu" Invoked="Back_KeyboardAccelerator_Invoked"/>
        <KeyboardAccelerator Key="Up" Modifiers="Menu" Invoked="Back_KeyboardAccelerator_Invoked"/>
        <KeyboardAccelerator Key="Home" Invoked="Home_KeyboardAccelerator_Invoked"/>
    </Grid.KeyboardAccelerators>

    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Grid ColumnSpacing="2" Height="31">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Image Source="/Assets/StoreLogo.png" Width="18" Margin="6,6,2,6"/>
        <TextBlock Text="设置" Grid.Column="2" FontSize="12" Margin="2,8,0,8"/>
    </Grid>
    
    <BreadcrumbBar Grid.Row="1" x:Name="breadcrumbBar" ItemClicked="BreadcrumbBar_ItemClicked" Margin="32,0,18,4">
        <BreadcrumbBar.ItemTemplate>
            <DataTemplate x:DataType="x:String">
                <TextBlock Text="{x:Bind}" Style="{StaticResource SubtitleTextBlockStyle}" FontWeight="Bold"/>
            </DataTemplate>
        </BreadcrumbBar.ItemTemplate>
    </BreadcrumbBar>
    <Frame x:Name="frame" Loaded="Frame_Loaded" Grid.Row="2" Margin="30,4,18,8"/>
</Grid>