<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="MainApp.Settings.SelectAppXDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MainApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="选择应用"
    CloseButtonText="取消"
    PrimaryButtonText="确认"
    DefaultButton="Primary"
    IsPrimaryButtonEnabled="False"
    Style="{StaticResource DefaultContentDialogStyle}"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    mc:Ignorable="d">

    <Grid RowSpacing="12" ColumnSpacing="8" MinWidth="380">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <AutoSuggestBox QueryIcon="Find"
                        x:Name="searchBox"
                        IsEnabled="False"
                        TextChanged="SearchBox_TextChanged"
                        QuerySubmitted="SearchBox_QuerySubmitted"
                        SuggestionChosen="SearchBox_SuggestionChosen"/>
        <Button Content="刷新"
                Grid.Column="1"
                Click="RefreshButton_Click"
                x:Name="refreshButton"
                IsEnabled="False"/>
        <ProgressBar Grid.ColumnSpan="2"
                      VerticalAlignment="Top"
                      IsIndeterminate="True"
                      x:Name="progressBar"/>

        <ScrollViewer Grid.Row="1" Grid.ColumnSpan="2" MinHeight="400">
            <ItemsControl x:Name="itemsControl"
                      IsEnabled="True"
                      Loaded="UI_Loaded"
                      Margin="0,0,8,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="local:AppXItem">
                        <CheckBox Tag="{x:Bind FamilyName}" Click="Select_CheckBox_Click">
                            <Grid Margin="0,4,0,4" VerticalAlignment="Stretch" MinWidth="344">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="64"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Image Source="{x:Bind Logo}" Grid.RowSpan="2"
                                       Stretch="None" Width="52" Height="52"
                                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{x:Bind DisplayName}"/>
                                <TextBlock Grid.Column="1" Grid.Row="1"
                                   Text="{x:Bind Publisher}"
                                   Style="{StaticResource CaptionTextBlockStyle}"/>
                            </Grid>
                        </CheckBox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</ContentDialog>
