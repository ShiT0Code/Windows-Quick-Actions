<?xml version="1.0" encoding="utf-8"?>
<Grid
    x:Class="TouchHelper.Settings.SettingsWindowUI"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TouchHelper.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Padding="8" Spacing="8">
            <Image Source="/Assets/StoreLogo.png" Height="20" Width="20"/>
            <TextBlock Text="设置" Margin="0,6,0,0"/>
        </StackPanel>
        <BreadcrumbBar x:Name="breadcrumbBar" Grid.Row="1" Margin="24,0,24,0" ItemClicked="BreadcrumbBar_ItemClicked">
            <BreadcrumbBar.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <TextBlock Text="{x:Bind}" Style="{StaticResource SubtitleTextBlockStyle}" FontWeight="Bold"/>
                </DataTemplate>
            </BreadcrumbBar.ItemTemplate>
        </BreadcrumbBar>

        <Frame x:Name="frame" Grid.Row="2" Loaded="Frame_Loaded" Margin="24,8,24,8"/>
    </Grid>
</Grid>
